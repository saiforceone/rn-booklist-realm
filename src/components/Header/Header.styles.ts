import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    flexDirection: 'row',
    padding: 10,
  },
  title: {
    flex: 1,
    fontSize: 18,
    fontWeight: '600',
  },
});
