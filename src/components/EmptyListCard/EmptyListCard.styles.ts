import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    height: 200,
    justifyContent: 'center',
    margin: 10,
    padding: 10,
  },
  mainText: {
    fontSize: 24,
    fontWeight: '600',
  },
});
